'use client';

import { useState } from 'react';

export default function Home() {
  const [activeTab, setActiveTab] = useState('dashboard');
  const [dockerStatus, setDockerStatus] = useState<string | null>(null);
  const [portStatus, setPortStatus] = useState<string | null>(null);
  const [resetStatus, setResetStatus] = useState<string | null>(null);

  const checkDocker = async () => {
    setDockerStatus('Checking...');
    try {
      const res = await fetch('/api/system/docker');
      const data = await res.json();
      setDockerStatus(JSON.stringify(data, null, 2));
    } catch (error) {
      setDockerStatus(`Error: ${error}`);
    }
  };

  const checkPorts = async () => {
    setPortStatus('Checking...');
    try {
      const res = await fetch('/api/system/ports');
      const data = await res.json();
      setPortStatus(JSON.stringify(data, null, 2));
    } catch (error) {
      setPortStatus(`Error: ${error}`);
    }
  };

  const resetSystem = async () => {
    if (!confirm('Are you sure you want to reset the system?')) return;
    setResetStatus('Resetting...');
    try {
      const res = await fetch('/api/system/reset', { method: 'POST' });
      const data = await res.json();
      setResetStatus(JSON.stringify(data, null, 2));
    } catch (error) {
      setResetStatus(`Error: ${error}`);
    }
  };

  return (
    <div className="min-h-screen bg-black text-white relative overflow-hidden">
      {/* Animated Background Grid */}
      <div className="fixed inset-0 bg-[linear-gradient(to_right,#0a0a0a_1px,transparent_1px),linear-gradient(to_bottom,#0a0a0a_1px,transparent_1px)] bg-[size:4rem_4rem] [mask-image:radial-gradient(ellipse_80%_50%_at_50%_0%,#000_70%,transparent_110%)] pointer-events-none" />
      
      {/* Ambient Glow Effects */}
      <div className="fixed top-0 left-1/4 w-96 h-96 bg-cyan-500/20 rounded-full blur-[120px] pointer-events-none animate-pulse" />
      <div className="fixed bottom-0 right-1/4 w-96 h-96 bg-emerald-500/20 rounded-full blur-[120px] pointer-events-none animate-pulse" style={{ animationDelay: '1s' }} />
      
      {/* Header with Advanced Glassmorphism */}
      <header className="relative border-b border-white/5 bg-black/40 backdrop-blur-2xl shadow-[0_8px_32px_rgba(0,255,255,0.1)]">
        <div className="container mx-auto px-8 py-5">
          <div className="flex items-center justify-between">
            <div className="flex items-center space-x-5">
              <div className="relative">
                <div className="absolute inset-0 bg-gradient-to-br from-cyan-500 via-emerald-500 to-cyan-600 rounded-2xl blur-xl opacity-60 animate-pulse" />
                <div className="relative h-14 w-14 rounded-2xl bg-gradient-to-br from-cyan-400 via-emerald-400 to-cyan-500 shadow-[0_0_30px_rgba(0,255,255,0.6)] flex items-center justify-center">
                  <div className="text-2xl">⚡</div>
                </div>
              </div>
              <div>
                <h1 className="text-4xl font-black tracking-tight bg-gradient-to-r from-cyan-300 via-emerald-300 to-cyan-400 bg-clip-text text-transparent drop-shadow-[0_0_20px_rgba(0,255,255,0.5)]">
                  Antigravity OS
                </h1>
                <p className="text-sm text-gray-400 font-medium tracking-wide">Autonomous Spec-to-Production Engine</p>
              </div>
            </div>
            <div className="flex items-center space-x-4">
              <div className="group relative px-5 py-2.5 rounded-xl bg-gradient-to-r from-emerald-500/10 to-cyan-500/10 border border-emerald-400/30 hover:border-emerald-400/60 transition-all duration-300 cursor-pointer">
                <div className="absolute inset-0 bg-gradient-to-r from-emerald-500/0 to-cyan-500/0 group-hover:from-emerald-500/20 group-hover:to-cyan-500/20 rounded-xl transition-all duration-300" />
                <div className="relative flex items-center space-x-3">
                  <div className="h-2.5 w-2.5 rounded-full bg-emerald-400 animate-pulse shadow-[0_0_12px_rgba(0,255,136,1)]" />
                  <span className="text-sm text-emerald-300 font-bold tracking-wide">SYSTEM ACTIVE</span>
                </div>
              </div>
            </div>
          </div>
        </div>
      </header>

      <div className="flex relative">
        {/* Sidebar with Modern Design */}
        <aside className="w-80 border-r border-white/5 bg-black/30 backdrop-blur-2xl min-h-screen relative">
          <div className="absolute inset-0 bg-gradient-to-b from-cyan-500/5 via-transparent to-emerald-500/5 pointer-events-none" />
          <nav className="relative p-8 space-y-2">
            {[
              { id: 'dashboard', label: 'Dashboard', icon: '📊', color: 'cyan', gradient: 'from-cyan-500 to-blue-500' },
              { id: 'specs', label: 'Spec Manager', icon: '📋', color: 'emerald', gradient: 'from-emerald-500 to-teal-500' },
              { id: 'execution', label: 'Task Execution', icon: '⚡', color: 'yellow', gradient: 'from-yellow-500 to-orange-500' },
              { id: 'infrastructure', label: 'Infrastructure', icon: '🏗️', color: 'red', gradient: 'from-red-500 to-pink-500' },
              { id: 'memory', label: 'Memory Graph', icon: '🧠', color: 'purple', gradient: 'from-purple-500 to-pink-500' },
              { id: 'logs', label: 'System Logs', icon: '📝', color: 'blue', gradient: 'from-blue-500 to-cyan-500' },
            ].map((item) => (
              <button
                key={item.id}
                onClick={() => setActiveTab(item.id)}
                className={`group relative w-full text-left px-6 py-4 rounded-2xl transition-all duration-300 ${
                  activeTab === item.id
                    ? 'bg-white/10 border border-white/20 shadow-[0_8px_32px_rgba(0,255,255,0.2)]'
                    : 'hover:bg-white/5 border border-transparent hover:border-white/10'
                }`}
              >
                {activeTab === item.id && (
                  <div className={`absolute inset-0 bg-gradient-to-r ${item.gradient} opacity-10 rounded-2xl`} />
                )}
                <div className="relative flex items-center space-x-4">
                  <div className={`text-2xl transition-transform duration-300 ${activeTab === item.id ? 'scale-110' : 'group-hover:scale-105'}`}>
                    {item.icon}
                  </div>
                  <span className={`font-semibold text-base transition-colors duration-300 ${
                    activeTab === item.id ? 'text-white' : 'text-gray-400 group-hover:text-gray-200'
                  }`}>
                    {item.label}
                  </span>
                </div>
                {activeTab === item.id && (
                  <div className={`absolute left-0 top-1/2 -translate-y-1/2 w-1 h-8 bg-gradient-to-b ${item.gradient} rounded-r-full shadow-[0_0_12px_currentColor]`} />
                )}
              </button>
            ))}
          </nav>
        </aside>

        {/* Main Content with Modern Layout */}
        <main className="flex-1 p-12 relative">
          {activeTab === 'dashboard' && (
            <div className="space-y-10">
              <div className="flex items-end justify-between">
                <div>
                  <h2 className="text-5xl font-black mb-3 bg-gradient-to-r from-cyan-300 via-emerald-300 to-cyan-400 bg-clip-text text-transparent drop-shadow-[0_0_30px_rgba(0,255,255,0.5)]">
                    System Dashboard
                  </h2>
                  <p className="text-gray-400 text-lg font-medium">Real-time monitoring and control</p>
                </div>
                <div className="text-sm text-gray-500 font-mono">Last updated: just now</div>
              </div>

              {/* Stats Grid with Modern Cards */}
              <div className="grid grid-cols-1 md:grid-cols-3 gap-6">
                {[
                  { label: 'Active Specs', value: '3', change: '+2', color: 'cyan', gradient: 'from-cyan-500/20 to-blue-500/20', icon: '📊' },
                  { label: 'Tasks Completed', value: '47', change: '+12', color: 'emerald', gradient: 'from-emerald-500/20 to-teal-500/20', icon: '✓' },
                  { label: 'Self-Healing Events', value: '12', change: '+3', color: 'purple', gradient: 'from-purple-500/20 to-pink-500/20', icon: '⚡' },
                ].map((stat) => (
                  <div
                    key={stat.label}
                    className="group relative p-8 rounded-3xl bg-white/5 border border-white/10 backdrop-blur-xl hover:bg-white/10 hover:border-white/20 transition-all duration-500 cursor-pointer overflow-hidden"
                  >
                    <div className={`absolute inset-0 bg-gradient-to-br ${stat.gradient} opacity-0 group-hover:opacity-100 transition-opacity duration-500`} />
                    <div className="relative">
                      <div className="flex items-center justify-between mb-4">
                        <p className="text-gray-400 text-sm font-bold uppercase tracking-widest">{stat.label}</p>
                        <div className="text-3xl opacity-20 group-hover:opacity-40 transition-opacity duration-300">{stat.icon}</div>
                      </div>
                      <div className="flex items-end justify-between">
                        <p className="text-6xl font-black text-white tracking-tight">{stat.value}</p>
                        <div className="flex items-center space-x-1 text-emerald-400 text-sm font-bold">
                          <span>↗</span>
                          <span>{stat.change}</span>
                        </div>
                      </div>
                    </div>
                  </div>
                ))}
              </div>

              {/* Recent Activity with Modern Design */}
              <div className="relative p-8 rounded-3xl bg-white/5 border border-white/10 backdrop-blur-xl overflow-hidden">
                <div className="absolute top-0 right-0 w-64 h-64 bg-cyan-500/10 rounded-full blur-3xl pointer-events-none" />
                <div className="relative">
                  <div className="flex items-center justify-between mb-8">
                    <h3 className="text-3xl font-black text-white">Recent Activity</h3>
                    <button className="px-4 py-2 rounded-xl bg-white/5 hover:bg-white/10 border border-white/10 hover:border-white/20 text-sm font-semibold text-gray-300 transition-all duration-300">
                      View All
                    </button>
                  </div>
                  <div className="space-y-3">
                    {[
                      { time: '2 min ago', event: 'Task 3.2 completed successfully', status: 'success', color: 'emerald', icon: '✓' },
                      { time: '15 min ago', event: 'Self-healing: Fixed type error in spec-parser', status: 'warning', color: 'yellow', icon: '⚡' },
                      { time: '1 hour ago', event: 'New spec created: user-authentication', status: 'info', color: 'cyan', icon: '📋' },
                    ].map((activity, i) => (
                      <div
                        key={i}
                        className="group flex items-center justify-between p-5 rounded-2xl bg-white/5 border border-white/10 hover:bg-white/10 hover:border-white/20 transition-all duration-300"
                      >
                        <div className="flex items-center space-x-5">
                          <div className="flex items-center justify-center w-10 h-10 rounded-xl bg-white/5 border border-white/10 group-hover:border-white/20 transition-all duration-300">
                            <span className="text-lg">{activity.icon}</span>
                          </div>
                          <div>
                            <p className="text-white font-semibold mb-1">{activity.event}</p>
                            <p className="text-gray-500 text-sm font-medium">{activity.time}</p>
                          </div>
                        </div>
                        <div className={`h-2 w-2 rounded-full bg-${activity.color}-400 shadow-[0_0_12px_currentColor] animate-pulse`} />
                      </div>
                    ))}
                  </div>
                </div>
              </div>
            </div>
          )}

          {activeTab === 'infrastructure' && (
            <div className="space-y-10">
              <div>
                <h2 className="text-5xl font-black mb-3 bg-gradient-to-r from-red-300 via-orange-300 to-red-400 bg-clip-text text-transparent drop-shadow-[0_0_30px_rgba(255,0,85,0.5)]">
                  Infrastructure Governance
                </h2>
                <p className="text-gray-400 text-lg font-medium">System control and monitoring</p>
              </div>

              {/* Control Panel with Modern Cards */}
              <div className="grid grid-cols-1 md:grid-cols-2 gap-6">
                <div className="group relative p-8 rounded-3xl bg-white/5 border border-cyan-500/20 backdrop-blur-xl hover:border-cyan-400/40 transition-all duration-500 overflow-hidden">
                  <div className="absolute inset-0 bg-gradient-to-br from-cyan-500/10 to-blue-500/10 opacity-0 group-hover:opacity-100 transition-opacity duration-500" />
                  <div className="relative">
                    <div className="flex items-center space-x-4 mb-6">
                      <div className="flex items-center justify-center w-14 h-14 rounded-2xl bg-cyan-500/10 border border-cyan-500/30">
                        <span className="text-3xl">🐳</span>
                      </div>
                      <h3 className="text-2xl font-black text-white">Docker Status</h3>
                    </div>
                    <button
                      onClick={checkDocker}
                      className="w-full px-6 py-4 rounded-2xl bg-gradient-to-r from-cyan-500/20 to-blue-500/20 text-cyan-300 border border-cyan-500/30 hover:border-cyan-400/60 hover:shadow-[0_0_40px_rgba(0,255,255,0.3)] transition-all duration-300 font-bold text-base group/btn"
                    >
                      <span className="flex items-center justify-center space-x-2">
                        <span>CHECK DOCKER STATUS</span>
                        <span className="group-hover/btn:translate-x-1 transition-transform duration-300">→</span>
                      </span>
                    </button>
                    {dockerStatus && (
                      <pre className="mt-6 p-5 rounded-2xl bg-black/60 text-cyan-300 text-xs overflow-auto max-h-48 border border-cyan-500/20 font-mono">
                        {dockerStatus}
                      </pre>
                    )}
                  </div>
                </div>

                <div className="group relative p-8 rounded-3xl bg-white/5 border border-emerald-500/20 backdrop-blur-xl hover:border-emerald-400/40 transition-all duration-500 overflow-hidden">
                  <div className="absolute inset-0 bg-gradient-to-br from-emerald-500/10 to-teal-500/10 opacity-0 group-hover:opacity-100 transition-opacity duration-500" />
                  <div className="relative">
                    <div className="flex items-center space-x-4 mb-6">
                      <div className="flex items-center justify-center w-14 h-14 rounded-2xl bg-emerald-500/10 border border-emerald-500/30">
                        <span className="text-3xl">🔌</span>
                      </div>
                      <h3 className="text-2xl font-black text-white">Port Management</h3>
                    </div>
                    <button
                      onClick={checkPorts}
                      className="w-full px-6 py-4 rounded-2xl bg-gradient-to-r from-emerald-500/20 to-teal-500/20 text-emerald-300 border border-emerald-500/30 hover:border-emerald-400/60 hover:shadow-[0_0_40px_rgba(0,255,136,0.3)] transition-all duration-300 font-bold text-base group/btn"
                    >
                      <span className="flex items-center justify-center space-x-2">
                        <span>CHECK PORT STATUS</span>
                        <span className="group-hover/btn:translate-x-1 transition-transform duration-300">→</span>
                      </span>
                    </button>
                    {portStatus && (
                      <pre className="mt-6 p-5 rounded-2xl bg-black/60 text-emerald-300 text-xs overflow-auto max-h-48 border border-emerald-500/20 font-mono">
                        {portStatus}
                      </pre>
                    )}
                  </div>
                </div>

                <div className="group relative p-8 rounded-3xl bg-white/5 border border-red-500/20 backdrop-blur-xl hover:border-red-400/40 transition-all duration-500 overflow-hidden md:col-span-2">
                  <div className="absolute inset-0 bg-gradient-to-br from-red-500/10 to-pink-500/10 opacity-0 group-hover:opacity-100 transition-opacity duration-500" />
                  <div className="relative">
                    <div className="flex items-center space-x-4 mb-6">
                      <div className="flex items-center justify-center w-14 h-14 rounded-2xl bg-red-500/10 border border-red-500/30">
                        <span className="text-3xl">🔄</span>
                      </div>
                      <div>
                        <h3 className="text-2xl font-black text-white">System Reset</h3>
                        <p className="text-gray-400 text-sm mt-1">Restore system to initial state</p>
                      </div>
                    </div>
                    <button
                      onClick={resetSystem}
                      className="w-full px-6 py-4 rounded-2xl bg-gradient-to-r from-red-500/20 to-pink-500/20 text-red-300 border border-red-500/30 hover:border-red-400/60 hover:shadow-[0_0_40px_rgba(255,0,85,0.3)] transition-all duration-300 font-bold text-base group/btn"
                    >
                      <span className="flex items-center justify-center space-x-2">
                        <span>RESET SYSTEM</span>
                        <span className="group-hover/btn:rotate-180 transition-transform duration-500">⟳</span>
                      </span>
                    </button>
                    {resetStatus && (
                      <pre className="mt-6 p-5 rounded-2xl bg-black/60 text-red-300 text-xs overflow-auto max-h-48 border border-red-500/20 font-mono">
                        {resetStatus}
                      </pre>
                    )}
                  </div>
                </div>
              </div>
            </div>
          )}

          {activeTab === 'specs' && (
            <div className="space-y-10">
              <div className="flex items-end justify-between">
                <div>
                  <h2 className="text-5xl font-black mb-3 bg-gradient-to-r from-emerald-300 via-teal-300 to-cyan-400 bg-clip-text text-transparent drop-shadow-[0_0_30px_rgba(0,255,136,0.5)]">
                    Spec Manager
                  </h2>
                  <p className="text-gray-400 text-lg font-medium">Feature specifications and requirements</p>
                </div>
                <button className="px-5 py-2.5 rounded-xl bg-emerald-500/10 hover:bg-emerald-500/20 border border-emerald-500/30 hover:border-emerald-400/50 text-emerald-300 font-semibold transition-all duration-300">
                  + New Spec
                </button>
              </div>

              <div className="grid grid-cols-1 md:grid-cols-3 gap-6">
                {[
                  { name: 'spec-orchestrator', status: 'Active', tasks: '14/14', progress: 100, color: 'emerald', gradient: 'from-emerald-500/20 to-teal-500/20' },
                  { name: 'user-authentication', status: 'Draft', tasks: '0/8', progress: 0, color: 'yellow', gradient: 'from-yellow-500/20 to-orange-500/20' },
                  { name: 'data-pipeline', status: 'Planning', tasks: '0/12', progress: 0, color: 'blue', gradient: 'from-blue-500/20 to-cyan-500/20' },
                ].map((spec, i) => (
                  <div
                    key={i}
                    className="group relative p-7 rounded-3xl bg-white/5 border border-white/10 backdrop-blur-xl hover:bg-white/10 hover:border-white/20 transition-all duration-500 overflow-hidden"
                  >
                    <div className={`absolute inset-0 bg-gradient-to-br ${spec.gradient} opacity-0 group-hover:opacity-100 transition-opacity duration-500`} />
                    <div className="relative">
                      <div className="flex items-center justify-between mb-5">
                        <div className="flex items-center space-x-3">
                          <div className={`w-3 h-3 rounded-full bg-${spec.color}-400 shadow-[0_0_12px_currentColor] animate-pulse`} />
                          <span className={`px-3 py-1 rounded-lg bg-${spec.color}-500/20 text-${spec.color}-300 text-xs font-bold uppercase tracking-wider`}>
                            {spec.status}
                          </span>
                        </div>
                      </div>
                      <h3 className="text-xl font-black text-white mb-4">{spec.name}</h3>
                      <div className="space-y-4">
                        <div className="flex justify-between items-center">
                          <span className="text-gray-400 text-sm font-medium">Progress</span>
                          <span className="text-white font-bold">{spec.tasks}</span>
                        </div>
                        <div className="relative w-full h-2 bg-white/5 rounded-full overflow-hidden">
                          <div
                            className={`absolute inset-y-0 left-0 bg-gradient-to-r ${spec.gradient.replace('/20', '')} rounded-full transition-all duration-1000`}
                            style={{ width: `${spec.progress}%` }}
                          />
                        </div>
                        <button className="w-full mt-3 px-4 py-3 rounded-xl bg-white/5 hover:bg-white/10 border border-white/10 hover:border-white/20 text-white font-semibold transition-all duration-300 group/btn">
                          <span className="flex items-center justify-center space-x-2">
                            <span>View Details</span>
                            <span className="group-hover/btn:translate-x-1 transition-transform duration-300">→</span>
                          </span>
                        </button>
                      </div>
                    </div>
                  </div>
                ))}
              </div>

              <div className="relative p-8 rounded-3xl bg-white/5 border border-white/10 backdrop-blur-xl overflow-hidden">
                <div className="absolute top-0 right-0 w-64 h-64 bg-emerald-500/10 rounded-full blur-3xl pointer-events-none" />
                <div className="relative">
                  <h3 className="text-3xl font-black text-white mb-8">Recent Spec Updates</h3>
                  <div className="space-y-3">
                    {[
                      { spec: 'spec-orchestrator', action: 'Task 3.2 completed', time: '5 min ago', icon: '✓', color: 'emerald' },
                      { spec: 'spec-orchestrator', action: 'Requirements updated', time: '1 hour ago', icon: '📝', color: 'cyan' },
                      { spec: 'user-authentication', action: 'Spec created', time: '2 hours ago', icon: '✨', color: 'yellow' },
                    ].map((update, i) => (
                      <div key={i} className="group flex items-center justify-between p-5 rounded-2xl bg-white/5 border border-white/10 hover:bg-white/10 hover:border-white/20 transition-all duration-300">
                        <div className="flex items-center space-x-4">
                          <div className="flex items-center justify-center w-10 h-10 rounded-xl bg-white/5 border border-white/10 group-hover:border-white/20 transition-all duration-300">
                            <span className="text-lg">{update.icon}</span>
                          </div>
                          <div>
                            <p className="text-white font-semibold">{update.spec}</p>
                            <p className="text-gray-400 text-sm">{update.action}</p>
                          </div>
                        </div>
                        <span className="text-sm text-gray-500 font-medium">{update.time}</span>
                      </div>
                    ))}
                  </div>
                </div>
              </div>
            </div>
          )}

          {activeTab === 'execution' && (
            <div className="space-y-10">
              <div>
                <h2 className="text-5xl font-black mb-3 bg-gradient-to-r from-yellow-300 via-orange-300 to-yellow-400 bg-clip-text text-transparent drop-shadow-[0_0_30px_rgba(255,200,0,0.5)]">
                  Task Execution
                </h2>
                <p className="text-gray-400 text-lg font-medium">Monitor and control task execution</p>
              </div>

              <div className="grid grid-cols-1 md:grid-cols-2 gap-6">
                <div className="group relative p-8 rounded-3xl bg-white/5 border border-yellow-500/20 backdrop-blur-xl hover:border-yellow-400/40 transition-all duration-500 overflow-hidden">
                  <div className="absolute inset-0 bg-gradient-to-br from-yellow-500/10 to-orange-500/10 opacity-0 group-hover:opacity-100 transition-opacity duration-500" />
                  <div className="relative">
                    <div className="flex items-center justify-between mb-6">
                      <h3 className="text-2xl font-black text-white">Current Task</h3>
                      <div className="px-3 py-1 rounded-lg bg-emerald-500/20 text-emerald-300 text-xs font-bold uppercase tracking-wider">
                        Completed
                      </div>
                    </div>
                    <div className="space-y-4">
                      <div className="flex justify-between items-center p-4 rounded-xl bg-white/5 border border-white/10">
                        <span className="text-gray-400 font-medium">Task ID</span>
                        <span className="text-yellow-300 font-mono font-bold text-lg">3.3</span>
                      </div>
                      <div className="flex justify-between items-center p-4 rounded-xl bg-white/5 border border-white/10">
                        <span className="text-gray-400 font-medium">Attempts</span>
                        <div className="flex items-center space-x-2">
                          <div className="flex space-x-1">
                            {[1, 2, 3].map((i) => (
                              <div key={i} className={`w-2 h-2 rounded-full ${i === 1 ? 'bg-emerald-400' : 'bg-gray-600'}`} />
                            ))}
                          </div>
                          <span className="text-cyan-300 font-bold">1/3</span>
                        </div>
                      </div>
                      <div className="p-5 rounded-xl bg-white/5 border border-white/10">
                        <p className="text-white font-medium leading-relaxed">Create atomic file writer with rollback support</p>
                      </div>
                    </div>
                  </div>
                </div>

                <div className="group relative p-8 rounded-3xl bg-white/5 border border-orange-500/20 backdrop-blur-xl hover:border-orange-400/40 transition-all duration-500 overflow-hidden">
                  <div className="absolute inset-0 bg-gradient-to-br from-orange-500/10 to-red-500/10 opacity-0 group-hover:opacity-100 transition-opacity duration-500" />
                  <div className="relative">
                    <div className="flex items-center justify-between mb-6">
                      <h3 className="text-2xl font-black text-white">Ralph-Loop</h3>
                      <div className="flex items-center space-x-2">
                        <div className="h-2 w-2 rounded-full bg-emerald-400 animate-pulse shadow-[0_0_12px_rgba(0,255,136,1)]" />
                        <span className="text-emerald-300 text-xs font-bold uppercase tracking-wider">Active</span>
                      </div>
                    </div>
                    <div className="space-y-2">
                      {['Build', 'Log', 'Analyze', 'Spec', 'Test'].map((step, i) => (
                        <div key={i} className={`flex items-center space-x-4 p-4 rounded-xl transition-all duration-300 ${
                          i < 3 ? 'bg-emerald-500/10 border border-emerald-500/30' : 'bg-white/5 border border-white/10'
                        }`}>
                          <div className="flex items-center justify-center w-8 h-8 rounded-lg bg-white/5 border border-white/10">
                            <div className={`w-3 h-3 rounded-full ${i < 3 ? 'bg-emerald-400 shadow-[0_0_8px_currentColor]' : 'bg-gray-600'}`} />
                          </div>
                          <span className={`font-semibold ${i < 3 ? 'text-emerald-300' : 'text-gray-500'}`}>{step}</span>
                        </div>
                      ))}
                    </div>
                  </div>
                </div>
              </div>

              <div className="relative p-8 rounded-3xl bg-white/5 border border-white/10 backdrop-blur-xl overflow-hidden">
                <div className="absolute top-0 left-0 w-64 h-64 bg-yellow-500/10 rounded-full blur-3xl pointer-events-none" />
                <div className="relative">
                  <h3 className="text-3xl font-black text-white mb-8">Task Queue</h3>
                  <div className="space-y-3">
                    {[
                      { id: '3.4', name: 'Implement task status transitions', status: 'queued', color: 'blue', icon: '⏳' },
                      { id: '3.5', name: 'Add Ralph-Loop integration', status: 'not_started', color: 'gray', icon: '○' },
                      { id: '4.1', name: 'Write unit tests', status: 'not_started', color: 'gray', icon: '○' },
                    ].map((task, i) => (
                      <div key={i} className="group flex items-center justify-between p-5 rounded-2xl bg-white/5 border border-white/10 hover:bg-white/10 hover:border-white/20 transition-all duration-300">
                        <div className="flex items-center space-x-5">
                          <div className="flex items-center justify-center w-12 h-12 rounded-xl bg-white/5 border border-white/10 group-hover:border-white/20 transition-all duration-300">
                            <span className="text-xl">{task.icon}</span>
                          </div>
                          <div>
                            <div className="flex items-center space-x-3 mb-1">
                              <span className="text-white font-mono font-bold">{task.id}</span>
                              <span className={`px-2 py-0.5 rounded bg-${task.color}-500/20 text-${task.color}-300 text-xs font-bold uppercase tracking-wider`}>
                                {task.status.replace('_', ' ')}
                              </span>
                            </div>
                            <p className="text-gray-400 font-medium">{task.name}</p>
                          </div>
                        </div>
                      </div>
                    ))}
                  </div>
                </div>
              </div>
            </div>
          )}
                  <h3 className="text-xl font-bold mb-4 text-orange-400">Ralph-Loop Status</h3>
                  <div className="space-y-3">
                    <div className="flex items-center gap-3">
                      <div className="h-3 w-3 rounded-full bg-emerald-500 animate-pulse shadow-[0_0_10px_rgba(0,255,136,0.8)]" />
                      <span className="text-emerald-400 font-semibold">Active</span>
                    </div>
                    <div className="space-y-2 mt-4">
                      {['Build', 'Log', 'Analyze', 'Spec', 'Test'].map((step, i) => (
                        <div key={i} className="flex items-center gap-3 p-2 rounded-lg bg-black/40">
                          <div className={`h-2 w-2 rounded-full ${i < 3 ? 'bg-emerald-500' : 'bg-gray-600'}`} />
                          <span className={`text-sm ${i < 3 ? 'text-emerald-400' : 'text-gray-500'}`}>{step}</span>
                        </div>
                      ))}
                    </div>
                  </div>
                </div>
              </div>

              <div className="p-8 rounded-2xl bg-gray-900/60 border border-yellow-500/30 backdrop-blur-xl shadow-[0_0_40px_rgba(255,255,0,0.1)]">
                <h3 className="text-2xl font-bold mb-6 text-yellow-400">Task Queue</h3>
                <div className="space-y-3">
                  {[
                    { id: '3.4', name: 'Implement task status transitions', status: 'queued', color: 'blue' },
                    { id: '3.5', name: 'Add Ralph-Loop integration', status: 'not_started', color: 'gray' },
                    { id: '4.1', name: 'Write unit tests', status: 'not_started', color: 'gray' },
                  ].map((task, i) => (
                    <div key={i} className={`flex items-center justify-between p-4 rounded-xl bg-black/40 border border-${task.color}-500/30 hover:border-${task.color}-400/50 transition-all`}>
                      <div className="flex items-center gap-4">
                        <span className={`text-${task.color}-400 font-mono font-bold`}>{task.id}</span>
                        <span className="text-gray-300">{task.name}</span>
                      </div>
                      <span className={`px-3 py-1 rounded-lg bg-${task.color}-500/20 text-${task.color}-400 text-sm font-semibold`}>
                        {task.status}
                      </span>
                    </div>
                  ))}
                </div>
              </div>
            </div>
          )}

          {activeTab === 'memory' && (
            <div className="space-y-10">
              <div>
                <h2 className="text-5xl font-black mb-3 bg-gradient-to-r from-purple-300 via-pink-300 to-purple-400 bg-clip-text text-transparent drop-shadow-[0_0_30px_rgba(168,85,247,0.5)]">
                  Memory Graph
                </h2>
                <p className="text-gray-400 text-lg font-medium">System knowledge and learning patterns</p>
              </div>

              <div className="grid grid-cols-1 md:grid-cols-2 gap-6">
                <div className="group relative p-8 rounded-3xl bg-white/5 border border-purple-500/20 backdrop-blur-xl hover:border-purple-400/40 transition-all duration-500 overflow-hidden">
                  <div className="absolute inset-0 bg-gradient-to-br from-purple-500/10 to-pink-500/10 opacity-0 group-hover:opacity-100 transition-opacity duration-500" />
                  <div className="relative">
                    <div className="flex items-center justify-between mb-6">
                      <h3 className="text-2xl font-black text-white">Success Patterns</h3>
                      <div className="px-3 py-1 rounded-lg bg-emerald-500/20 text-emerald-300 text-xs font-bold">
                        {3} ACTIVE
                      </div>
                    </div>
                    <div className="space-y-3">
                      {[
                        { pattern: 'Atomic file operations', uses: 47, success: 100, icon: '⚛️' },
                        { pattern: 'Type-safe validation', uses: 32, success: 98, icon: '🛡️' },
                        { pattern: 'Error recovery', uses: 12, success: 83, icon: '🔄' },
                      ].map((item, i) => (
                        <div key={i} className="p-5 rounded-2xl bg-white/5 border border-white/10 hover:bg-white/10 hover:border-white/20 transition-all duration-300">
                          <div className="flex items-start justify-between mb-3">
                            <div className="flex items-center space-x-3">
                              <span className="text-2xl">{item.icon}</span>
                              <span className="text-white font-semibold">{item.pattern}</span>
                            </div>
                            <span className="text-emerald-300 font-black text-lg">{item.success}%</span>
                          </div>
                          <div className="flex items-center justify-between">
                            <span className="text-gray-400 text-sm font-medium">Usage count</span>
                            <span className="text-purple-300 font-bold">{item.uses}x</span>
                          </div>
                        </div>
                      ))}
                    </div>
                  </div>
                </div>

                <div className="group relative p-8 rounded-3xl bg-white/5 border border-pink-500/20 backdrop-blur-xl hover:border-pink-400/40 transition-all duration-500 overflow-hidden">
                  <div className="absolute inset-0 bg-gradient-to-br from-pink-500/10 to-red-500/10 opacity-0 group-hover:opacity-100 transition-opacity duration-500" />
                  <div className="relative">
                    <div className="flex items-center justify-between mb-6">
                      <h3 className="text-2xl font-black text-white">Failed Patterns</h3>
                      <div className="px-3 py-1 rounded-lg bg-red-500/20 text-red-300 text-xs font-bold">
                        {3} AVOIDED
                      </div>
                    </div>
                    <div className="space-y-3">
                      {[
                        { pattern: 'Complex parsing without tests', failures: 3, icon: '⚠️' },
                        { pattern: 'Missing type definitions', failures: 2, icon: '❌' },
                        { pattern: 'Incomplete error handling', failures: 1, icon: '🚫' },
                      ].map((item, i) => (
                        <div key={i} className="p-5 rounded-2xl bg-white/5 border border-white/10 hover:bg-white/10 hover:border-white/20 transition-all duration-300">
                          <div className="flex items-start justify-between mb-3">
                            <div className="flex items-center space-x-3">
                              <span className="text-2xl">{item.icon}</span>
                              <span className="text-white font-semibold">{item.pattern}</span>
                            </div>
                            <span className="text-red-300 font-black text-lg">{item.failures}x</span>
                          </div>
                          <div className="text-gray-400 text-sm font-medium">Avoided in future tasks</div>
                        </div>
                      ))}
                    </div>
                  </div>
                </div>
              </div>

              <div className="relative p-8 rounded-3xl bg-white/5 border border-white/10 backdrop-blur-xl overflow-hidden">
                <div className="absolute top-0 right-0 w-64 h-64 bg-purple-500/10 rounded-full blur-3xl pointer-events-none" />
                <div className="relative">
                  <h3 className="text-3xl font-black text-white mb-8">Learning Insights</h3>
                  <div className="space-y-4">
                    {[
                      { insight: 'Atomic operations prevent 100% of partial write errors', impact: 'High', color: 'emerald', icon: '💎' },
                      { insight: 'Property-based testing catches 3x more edge cases', impact: 'High', color: 'emerald', icon: '🎯' },
                      { insight: 'Type-safe validation reduces runtime errors by 95%', impact: 'Medium', color: 'yellow', icon: '🔒' },
                    ].map((item, i) => (
                      <div key={i} className="group flex items-start space-x-5 p-6 rounded-2xl bg-white/5 border border-white/10 hover:bg-white/10 hover:border-white/20 transition-all duration-300">
                        <div className="flex items-center justify-center w-12 h-12 rounded-xl bg-white/5 border border-white/10 group-hover:border-white/20 transition-all duration-300 flex-shrink-0">
                          <span className="text-2xl">{item.icon}</span>
                        </div>
                        <div className="flex-1">
                          <p className="text-white font-medium leading-relaxed mb-3">{item.insight}</p>
                          <span className={`inline-block px-3 py-1 rounded-lg bg-${item.color}-500/20 text-${item.color}-300 text-xs font-bold uppercase tracking-wider`}>
                            {item.impact} IMPACT
                          </span>
                        </div>
                      </div>
                    ))}
                  </div>
                </div>
              </div>
            </div>
          )}

          {activeTab === 'logs' && (
            <div className="space-y-10">
              <div className="flex items-end justify-between">
                <div>
                  <h2 className="text-5xl font-black mb-3 bg-gradient-to-r from-blue-300 via-cyan-300 to-blue-400 bg-clip-text text-transparent drop-shadow-[0_0_30px_rgba(59,130,246,0.5)]">
                    System Logs
                  </h2>
                  <p className="text-gray-400 text-lg font-medium">Real-time system activity and events</p>
                </div>
                <div className="flex items-center space-x-3">
                  <div className="px-4 py-2 rounded-xl bg-white/5 border border-white/10">
                    <span className="text-gray-400 text-sm font-medium">Live</span>
                  </div>
                  <div className="h-2 w-2 rounded-full bg-emerald-400 animate-pulse shadow-[0_0_12px_rgba(0,255,136,1)]" />
                </div>
              </div>

              <div className="relative p-8 rounded-3xl bg-white/5 border border-white/10 backdrop-blur-xl overflow-hidden">
                <div className="absolute top-0 left-0 w-64 h-64 bg-blue-500/10 rounded-full blur-3xl pointer-events-none" />
                <div className="relative">
                  <div className="flex items-center justify-between mb-8">
                    <h3 className="text-3xl font-black text-white">Activity Stream</h3>
                    <div className="flex gap-2">
                      {[
                        { label: 'All', color: 'white' },
                        { label: 'Success', color: 'emerald' },
                        { label: 'Warning', color: 'yellow' },
                        { label: 'Error', color: 'red' },
                      ].map((filter) => (
                        <button
                          key={filter.label}
                          className={`px-4 py-2 rounded-xl bg-${filter.color === 'white' ? 'white' : filter.color + '-500'}/10 hover:bg-${filter.color === 'white' ? 'white' : filter.color + '-500'}/20 border border-${filter.color === 'white' ? 'white' : filter.color + '-500'}/20 hover:border-${filter.color === 'white' ? 'white' : filter.color + '-500'}/40 text-${filter.color === 'white' ? 'white' : filter.color + '-300'} transition-all text-sm font-semibold`}
                        >
                          {filter.label}
                        </button>
                      ))}
                    </div>
                  </div>
                  <div className="space-y-2 max-h-[500px] overflow-y-auto pr-2 custom-scrollbar">
                    {[
                      { time: '14:32:15', level: 'success', message: 'Task 3.2 completed successfully', icon: '✓' },
                      { time: '14:31:48', level: 'info', message: 'Ralph-Loop attempt 1/3 started', icon: 'ℹ' },
                      { time: '14:31:22', level: 'success', message: 'Atomic write completed: tasks.md', icon: '✓' },
                      { time: '14:30:55', level: 'warning', message: 'Test retry required for spec-parser', icon: '⚠' },
                      { time: '14:30:12', level: 'info', message: 'Dependency graph built: 14 tasks', icon: 'ℹ' },
                      { time: '14:29:45', level: 'success', message: 'Spec loaded: spec-orchestrator', icon: '✓' },
                      { time: '14:29:18', level: 'info', message: 'System initialized', icon: '🚀' },
                    ].map((log, i) => (
                      <div
                        key={i}
                        className="group flex items-start gap-4 p-5 rounded-2xl bg-white/5 border border-white/10 hover:bg-white/10 hover:border-white/20 transition-all duration-300"
                      >
                        <span className="text-gray-500 font-mono text-sm font-medium whitespace-nowrap mt-0.5">{log.time}</span>
                        <div className="flex items-center justify-center w-8 h-8 rounded-lg bg-white/5 border border-white/10 group-hover:border-white/20 transition-all duration-300 flex-shrink-0">
                          <span className="text-base">{log.icon}</span>
                        </div>
                        <span className="text-white font-medium flex-1 leading-relaxed">{log.message}</span>
                        <span className={`px-3 py-1 rounded-lg bg-${
                          log.level === 'success' ? 'emerald' : log.level === 'warning' ? 'yellow' : log.level === 'error' ? 'red' : 'blue'
                        }-500/20 text-${
                          log.level === 'success' ? 'emerald' : log.level === 'warning' ? 'yellow' : log.level === 'error' ? 'red' : 'blue'
                        }-300 text-xs font-bold uppercase tracking-wider whitespace-nowrap`}>
                          {log.level}
                        </span>
                      </div>
                    ))}
                  </div>
                </div>
              </div>

              <div className="grid grid-cols-1 md:grid-cols-3 gap-6">
                {[
                  { label: 'Total Events', value: '1,247', icon: '📊', color: 'blue', gradient: 'from-blue-500/20 to-cyan-500/20' },
                  { label: 'Errors', value: '3', icon: '❌', color: 'red', gradient: 'from-red-500/20 to-pink-500/20' },
                  { label: 'Warnings', value: '12', icon: '⚠️', color: 'yellow', gradient: 'from-yellow-500/20 to-orange-500/20' },
                ].map((stat, i) => (
                  <div
                    key={i}
                    className="group relative p-7 rounded-3xl bg-white/5 border border-white/10 backdrop-blur-xl hover:bg-white/10 hover:border-white/20 transition-all duration-500 overflow-hidden"
                  >
                    <div className={`absolute inset-0 bg-gradient-to-br ${stat.gradient} opacity-0 group-hover:opacity-100 transition-opacity duration-500`} />
                    <div className="relative">
                      <div className="flex items-center justify-between mb-4">
                        <p className="text-gray-400 text-sm font-bold uppercase tracking-widest">{stat.label}</p>
                        <span className="text-3xl opacity-30 group-hover:opacity-50 transition-opacity duration-300">{stat.icon}</span>
                      </div>
                      <p className="text-5xl font-black text-white tracking-tight">{stat.value}</p>
                    </div>
                  </div>
                ))}
              </div>
            </div>
          )}              <div className="grid grid-cols-1 md:grid-cols-3 gap-6">
                {[
                  { label: 'Total Events', value: '1,247', color: 'blue' },
                  { label: 'Errors', value: '3', color: 'red' },
                  { label: 'Warnings', value: '12', color: 'yellow' },
                ].map((stat, i) => (
                  <div
                    key={i}
                    className={`p-6 rounded-2xl bg-gray-900/60 border border-${stat.color}-500/30 backdrop-blur-xl hover:border-${stat.color}-400/50 transition-all`}
                  >
                    <p className="text-gray-400 text-sm mb-2 uppercase tracking-wider">{stat.label}</p>
                    <p className={`text-4xl font-bold text-${stat.color}-400`}>{stat.value}</p>
                  </div>
                ))}
              </div>
            </div>
          )}
        </main>
      </div>
    </div>
  );
}
                      </div>
                      <p className="text-5xl font-black text-white tracking-tight">{stat.value}</p>
                    </div>
                  </div>
                ))}
              </div>
            </div>
          )}
        </main>
      </div>
    </div>
  );
}
